{
    "name": "vgg-site",
    "version": "1.0.0",
    "description": "Veteran Gutters & Guards Website with AI Chatbot",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "build": "mkdir -p dist && cp *.html *.css *.js *.xml *.txt *.json dist/ 2>/dev/null || : && cp -r assets dist/ 2>/dev/null || : && cp -r functions dist/ 2>/dev/null || : && cp -r locations dist/ 2>/dev/null || : && cp -r services dist/ 2>/dev/null || : && cp -r admin dist/ 2>/dev/null || :"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "dependencies": {
        "@supabase/supabase-js": "^2.39.0",
        "googleapis": "^129.0.0",
        "openai": "^4.20.1"
    }
}